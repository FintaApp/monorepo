table:
  name: plaid_institutions
  schema: public
object_relationships:
  - name: logo_file
    using:
      foreign_key_constraint_on: logo_file_id
insert_permissions:
  - role: user
    permission:
      check: {}
      columns:
        - id
        - name
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - name
      filter: {}
update_permissions:
  - role: user
    permission:
      columns:
        - name
      filter: {}
      check: {}
